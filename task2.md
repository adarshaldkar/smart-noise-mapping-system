# Backend Codebase Audit Tasks

- `[/]` Clean up root directory (remove garbage files and unused folders like `__pycache__`, `android`, etc. if irrelevant)
- `[ ]` Audit `api/` directory (review `main.py`, `Dockerfile`, `requirements.txt`, clean up `folium/` if unused)
- `[ ]` Audit `consumer/` directory (review `main.py`, `ml_classifier.py`, `cal.py`, `Dockerfile`)
- `[ ]` Audit `scripts/` and test files (`publish_sample_data.py`, `test_system_integration.py`, etc.)
- `[ ]` Audit infrastructure (`docker-compose.yml`, Makefiles)
- `[ ]` Verify end-to-end integration logic and error handling
- `[ ]` Present Final Audit Report and transition to Frontend

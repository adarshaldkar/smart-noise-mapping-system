# Copy of .env.template with example values for local testing
# If you're using a broker on the same Docker network (recommended), set MQTT_HOST to the container name (e.g. mqtt)
# If using a broker on the host machine, set MQTT_HOST=host.docker.internal

MQTT_HOST=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
